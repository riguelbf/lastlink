namespace Presentation.Endpoints.AdvanceRequest;

public class AdvanceRequestEndpoint : EndpointBase
{
    public override void MapEndpoint(IEndpointRouteBuilder app)
    {
        // Initialize all endpoint classes
        // var getEndpoint = new GetAdvanceRequestEndpoint();
        // var getByCreatorEndpoint = new GetAdvanceRequestsByCreatorEndpoint();
        // var createEndpoint = new CreateAdvanceRequestEndpoint();
        // var approveEndpoint = new ApproveAdvanceRequestEndpoint();
        // var rejectEndpoint = new RejectAdvanceRequestEndpoint();
        //
        // // Map endpoints
        // getEndpoint.MapEndpoint(app);
        // getByCreatorEndpoint.MapEndpoint(app);
        // createEndpoint.MapEndpoint(app);
        // approveEndpoint.MapEndpoint(app);
        // rejectEndpoint.MapEndpoint(app);
    }
}